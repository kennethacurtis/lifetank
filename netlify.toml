[build] 
  publish = "docs"
  command = "hugo"

[context.production.environment]
  HUGO_VERSION = "0.58.3" # if older, use format: 0.53 (no trailing zero)
  HUGO_ENV = "production"
  HUGO_ENABLEGITINFO = "true"